/*** GENERATED FROM ${info.inputFile} - DO NOT EDIT  ***/

/*
 * ${className}.java
 *
 * Copyright (c) 2013 Rhythm & Hues Studios. All rights reserved.
 */
package ${info.packageName};

public interface ${className} {
#foreach($method in $info.methodInfos)

    /**
     * ${method.description}
     *
#foreach($param in $method.parameters)
     * @param ${param.name}
#end
     * @return ${method.javadocConvertedReturnType}
     * @throws ${method.throwsClause}
     */
#if ( $method.isDeprecated() )
    @Deprecated
#end
    ${method.convertedReturnType} ${method.name}(${method.convertedParamString}) throws ${method.throwsClause};
#end
}