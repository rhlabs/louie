/*** GENERATED FROM ${info.inputFile} - DO NOT EDIT  ***/

/*
 * ${className}.java
 *
 * Copyright (c) 2013 Rhythm & Hues Studios. All rights reserved.
 */
package ${info.packageName};

import com.rhythm.louie.Delegate;

public class ${className} implements ${baseName}Client, Delegate<${baseName}Client> {
    private ${baseName}Client delegate;
    public ${className}() {}

    @Override
    public void setDelegate(${baseName}Client delegate) {
        this.delegate = delegate;
    }

    @Override
    public ${baseName}Client getDelegate() {
        return delegate;
    }
#foreach($method in $info.methodInfos)

#if ( $method.isDeprecated() )
    @Deprecated
#end
    @Override
    public ${method.convertedReturnType} ${method.name}(${method.convertedParamString}) throws ${method.throwsClause} {
        return delegate.${method.name}(${method.paramNameString});
    }
#end
}